# Apache Superset Environment Configuration Template
# この環境ファイルをコピーして使用してください
# Copy this file to .env and customize for your environment

# 使用する環境ファイル:
# - .env.production  : 本番環境（Azure PostgreSQL使用）
# - .env.development : 開発環境（ローカルPostgreSQL使用）
# - .env.sandbox     : サンドボックス環境（ローカルPostgreSQL使用、ポート番号+1）

# クイックスタート:
# cp .env.development .env  # 開発環境を使用する場合
# cp .env.sandbox .env      # サンドボックス環境を使用する場合
# cp .env.production .env   # 本番環境を使用する場合（Azure PostgreSQL設定が必要）

# Environment Name
SUPERSET_ENV=development

# Container Names
SUPERSET_CONTAINER_NAME=superset-dev
REDIS_CONTAINER_NAME=superset-redis-dev
POSTGRES_CONTAINER_NAME=superset-postgres-dev

# Network Name
NETWORK_NAME=superset-network-dev

# Volume Names
REDIS_VOLUME_NAME=superset-redis-data-dev
POSTGRES_VOLUME_NAME=superset-postgres-data-dev
SUPERSET_VOLUME_NAME=superset-home-dev

# Port Configuration
SUPERSET_PORT=8088
REDIS_PORT=6379
POSTGRES_PORT=5432

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT_INTERNAL=6379

# Database Configuration
DATABASE_DIALECT=postgresql
DATABASE_USER=superset
DATABASE_PASSWORD=superset
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_DB=superset

# Local PostgreSQL Configuration (for development/sandbox)
POSTGRES_USER=superset
POSTGRES_PASSWORD=superset
POSTGRES_DB=superset

# Superset Security
SUPERSET_SECRET_KEY=CHANGE_THIS_TO_A_COMPLEX_RANDOM_SECRET_KEY

# Superset Configuration
SUPERSET_LOAD_EXAMPLES=yes

# Language Configuration
BABEL_DEFAULT_LOCALE=ja
LANGUAGES=ja,en

# Admin User Configuration
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin
ADMIN_FIRSTNAME=管理者
ADMIN_LASTNAME=ユーザー
