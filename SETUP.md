# Apache Superset 5.0 Podman/Docker セットアップガイド

Apache Superset 5.0、Redis 8.2、PostgreSQL 16を使用した完全なBI環境のセットアップガイドです。

## 📋 目次

- [概要](#概要)
- [前提条件](#前提条件)
- [環境構成](#環境構成)
- [セットアップ手順](#セットアップ手順)
- [使用方法](#使用方法)
- [トラブルシューティング](#トラブルシューティング)

## 🎯 概要

このリポジトリには、以下の3つの環境用のPodman/Docker設定ファイルが含まれています：

1. **本番環境** (Production)
2. **開発環境** (Development)
3. **開発サンドボックス環境** (Development Sandbox)

### 主な機能

- ✅ Apache Superset 5.0.0
- ✅ Redis 8.2系（キャッシュサーバー）
- ✅ PostgreSQL 16系（メタデータストア）
- ✅ 日本語UIデフォルト設定
- ✅ 日本語/英語切り替え対応
- ✅ 環境ごとの独立したコンテナとボリューム
- ✅ Azure PostgreSQL接続対応（本番環境）

## 📦 前提条件

以下のいずれかがインストールされている必要があります：

- Docker Desktop 20.10以上
- Podman 4.0以上 + podman-compose

### インストール確認

```bash
# Dockerの場合
docker --version
docker-compose --version

# Podmanの場合
podman --version
podman-compose --version
```

## 🏗️ 環境構成

### ポート番号

| 環境 | Superset | Redis | PostgreSQL |
|------|----------|-------|------------|
| 本番環境 (Production) | 8088 | 6379 | 5432* |
| 開発環境 (Development) | 8088 | 6379 | 5432 |
| サンドボックス (Sandbox) | 8089 | 6380 | 5433 |

*本番環境ではAzure PostgreSQLへの外部接続を想定しているため、ローカルPostgreSQLは起動しません。

### コンテナ名

| 環境 | Superset | Redis | PostgreSQL |
|------|----------|-------|------------|
| 本番環境 | superset-prod | superset-redis-prod | superset-postgres-prod |
| 開発環境 | superset-dev | superset-redis-dev | superset-postgres-dev |
| サンドボックス | superset-sandbox | superset-redis-sandbox | superset-postgres-sandbox |

## 🚀 セットアップ手順

### 1. リポジトリのクローン

```bash
git clone <repository-url>
cd ApacheSupersetSandbox
```

### 2. 環境ファイルの設定

使用したい環境に応じて`.env`ファイルを編集します：

#### 本番環境の場合

```bash
cp .env.production .env
```

`.env`ファイルを編集し、以下の設定を変更してください：

```bash
# Azure PostgreSQL接続情報
DATABASE_USER=your_azure_postgres_user
DATABASE_PASSWORD=your_azure_postgres_password
DATABASE_HOST=your-azure-postgres-server.postgres.database.azure.com

# セキュリティキー（必ず変更してください）
SUPERSET_SECRET_KEY=your_complex_random_secret_key_here

# 管理者パスワード（必ず変更してください）
ADMIN_PASSWORD=your_secure_password_here
```

#### 開発環境の場合

```bash
cp .env.development .env
```

デフォルト設定で動作しますが、必要に応じてセキュリティキーを変更してください。

#### サンドボックス環境の場合

```bash
cp .env.sandbox .env
```

デフォルト設定で動作します。開発環境と並行して実行できます。

### 3. コンテナの起動

#### Dockerの場合

**本番環境（Azure PostgreSQL使用）:**
```bash
docker-compose --env-file .env.production up -d superset redis
```

**開発環境:**
```bash
docker-compose --env-file .env.development --profile with-local-db up -d
```

**サンドボックス環境:**
```bash
docker-compose --env-file .env.sandbox --profile with-local-db up -d
```

#### Podmanの場合

**本番環境（Azure PostgreSQL使用）:**
```bash
podman-compose --env-file .env.production up -d superset redis
```

**開発環境:**
```bash
podman-compose --env-file .env.development --profile with-local-db up -d
```

**サンドボックス環境:**
```bash
podman-compose --env-file .env.sandbox --profile with-local-db up -d
```

### 4. 初回起動の確認

初回起動時は、データベースの初期化とサンプルデータのロード（開発/サンドボックス環境）が実行されます。
起動完了まで2-3分かかる場合があります。

```bash
# ログの確認
docker-compose logs -f superset
# または
podman-compose logs -f superset
```

"Starting Superset..."というメッセージが表示されたら起動完了です。

## 💻 使用方法

### アクセス方法

ブラウザで以下のURLにアクセスします：

- **本番環境:** http://localhost:8088
- **開発環境:** http://localhost:8088
- **サンドボックス環境:** http://localhost:8089

### ログイン情報

デフォルトの管理者アカウント：

| 項目 | 本番環境 | 開発/サンドボックス |
|------|----------|-------------------|
| ユーザー名 | admin | admin |
| パスワード | `.env`で設定 | admin |

**⚠️ 本番環境では必ずパスワードを変更してください！**

### 言語の切り替え

1. 画面右上のユーザーアイコンをクリック
2. 「Settings」または「設定」を選択
3. 言語設定から「日本語」または「English」を選択

### コンテナの停止

```bash
# Dockerの場合
docker-compose --env-file .env.<environment> down

# Podmanの場合
podman-compose --env-file .env.<environment> down
```

データを保持したまま停止されます。

### データの完全削除

```bash
# コンテナとボリュームを削除
docker-compose --env-file .env.<environment> down -v

# または
podman-compose --env-file .env.<environment> down -v
```

## 🔧 高度な設定

### 複数環境の同時実行

開発環境とサンドボックス環境は異なるポート番号を使用するため、同時に実行できます：

```bash
# 開発環境起動
docker-compose --env-file .env.development --profile with-local-db up -d

# サンドボックス環境起動（別のターミナルまたは同時に実行可能）
docker-compose --env-file .env.sandbox --profile with-local-db up -d
```

### カスタム設定

`superset/superset_config.py`ファイルを編集することで、Supersetの詳細設定をカスタマイズできます：

- キャッシュ設定
- セッション設定
- SQL Labの設定
- セキュリティ設定
- 機能フラグ

設定変更後は、コンテナを再ビルドしてください：

```bash
docker-compose --env-file .env.<environment> build superset
docker-compose --env-file .env.<environment> up -d
```

### Azure PostgreSQL接続のSSL設定

Azure PostgreSQLを使用する場合、SSL接続が自動的に有効になります。
`superset_config.py`で以下の設定が適用されます：

```python
if 'azure' in os.getenv('DATABASE_HOST', '').lower():
    SQLALCHEMY_DATABASE_URI += '?sslmode=require'
```

## 🐛 トラブルシューティング

### データベース接続エラー

**症状:** "Database connection timeout" エラーが表示される

**解決方法:**
1. PostgreSQLが起動しているか確認
   ```bash
   docker-compose ps
   ```
2. 接続情報が正しいか`.env`ファイルを確認
3. Azure PostgreSQLの場合、ファイアウォール設定を確認

### ポート番号の競合

**症状:** "port is already allocated" エラーが表示される

**解決方法:**
1. 使用されているポートを確認
   ```bash
   # Linux/Mac
   sudo lsof -i :8088
   
   # Windows
   netstat -ano | findstr :8088
   ```
2. `.env`ファイルでポート番号を変更

### 日本語が表示されない

**症状:** UIが日本語にならない

**解決方法:**
1. ブラウザのキャッシュをクリア
2. 設定画面で言語を「日本語」に変更
3. 一度ログアウトして再ログイン

### コンテナが起動しない

**症状:** コンテナがすぐに停止する

**解決方法:**
1. ログを確認
   ```bash
   docker-compose logs superset
   ```
2. `.env`ファイルの設定を確認
3. 必要に応じてコンテナを再ビルド
   ```bash
   docker-compose build --no-cache
   ```

## 📝 その他の情報

### セキュリティに関する注意事項

1. **本番環境では必ず以下を変更してください:**
   - `SUPERSET_SECRET_KEY`
   - `ADMIN_PASSWORD`
   - データベースのパスワード

2. **ファイアウォール設定:**
   - 本番環境では適切なファイアウォール設定を行ってください
   - 不要なポートは外部に公開しないでください

3. **SSL/TLS:**
   - 本番環境ではリバースプロキシ（Nginx等）を使用してHTTPSを設定することを推奨します

### バックアップとリストア

**バックアップ:**
```bash
# PostgreSQLデータのバックアップ
docker exec superset-postgres-dev pg_dump -U superset superset > backup.sql

# Supersetホームディレクトリのバックアップ
docker run --rm -v superset-home-dev:/data -v $(pwd):/backup alpine tar czf /backup/superset-home-backup.tar.gz /data
```

**リストア:**
```bash
# PostgreSQLデータのリストア
docker exec -i superset-postgres-dev psql -U superset superset < backup.sql
```

## 📄 ライセンス

このプロジェクトの設定ファイルはMITライセンスの下で提供されています。
ただし、使用されるソフトウェア（Apache Superset、Redis、PostgreSQL）はそれぞれのライセンスに従います。

- Apache Superset: Apache License 2.0
- Redis: BSD 3-Clause License
- PostgreSQL: PostgreSQL License

## 🤝 サポート

問題が発生した場合は、以下を確認してください：

1. このドキュメントのトラブルシューティングセクション
2. Apache Supersetの公式ドキュメント: https://superset.apache.org/docs/intro
3. GitHubのIssuesセクション

## 🔄 更新履歴

- 2024-12-31: 初版リリース
  - Apache Superset 5.0対応
  - Redis 8.2対応
  - PostgreSQL 16対応
  - 3環境（本番/開発/サンドボックス）対応
  - 日本語対応
