# Production Environment Configuration for Apache Superset
# 本番環境設定

# Environment Name
SUPERSET_ENV=production

# Container Names (本番環境)
SUPERSET_CONTAINER_NAME=superset-prod
REDIS_CONTAINER_NAME=superset-redis-prod
POSTGRES_CONTAINER_NAME=superset-postgres-prod

# Network Name
NETWORK_NAME=superset-network-prod

# Volume Names
REDIS_VOLUME_NAME=superset-redis-data-prod
POSTGRES_VOLUME_NAME=superset-postgres-data-prod
SUPERSET_VOLUME_NAME=superset-home-prod

# Port Configuration (Default Ports)
SUPERSET_PORT=8088
REDIS_PORT=6379
POSTGRES_PORT=5432

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT_INTERNAL=6379

# Database Configuration (External Azure PostgreSQL)
# Azure PostgreSQL接続設定
DATABASE_DIALECT=postgresql
DATABASE_USER=your_azure_postgres_user
DATABASE_PASSWORD=your_azure_postgres_password
DATABASE_HOST=your-azure-postgres-server.postgres.database.azure.com
DATABASE_PORT=5432
DATABASE_DB=superset

# Note: For production with external Azure PostgreSQL, use:
# docker-compose --env-file .env.production up -d superset redis
# (This excludes the local postgres service)

# Superset Security
SUPERSET_SECRET_KEY=CHANGE_THIS_TO_A_COMPLEX_RANDOM_SECRET_KEY_FOR_PRODUCTION

# Superset Configuration
SUPERSET_LOAD_EXAMPLES=no

# Language Configuration (Japanese as default)
BABEL_DEFAULT_LOCALE=ja
LANGUAGES=ja,en

# Admin User Configuration
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=CHANGE_THIS_PASSWORD
ADMIN_FIRSTNAME=管理者
ADMIN_LASTNAME=ユーザー
